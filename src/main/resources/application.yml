spring:
  application:
    name: ecclesia-sms

  datasource:
    url: jdbc:mysql://localhost:3306/ecclesia_sms_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "@Emmanuel3540"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  async:
    core-pool-size: 5
    max-pool-size: 20
    thread-name-prefix: sms-worker-

server:
  port: 8080

cloud:
  aws:
    credentials:
      access-key: your_access_key
      secret-key: your_secret_key
    region:
      static: eu-west-1
    stack:
      auto: false

logging:
  level:
    software.amazon.awssdk: INFO
    com.ayitech: DEBUG
